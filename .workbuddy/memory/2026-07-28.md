# 2026-07-28

- 修改 `structural/bridge-pattern.html`：为第 6 节 TypeScript 示例与第 7 节 Python 示例补充逐行中文注解（注释说明 Abstraction/Implementor 各角色职责与组合连接方式）；同时修正第 6 节正文里残留的错误「第 34 行」行号引用。AGENTS.md 要求每篇笔记 TS/Python 示例需解释关键类职责，此前该文件代码块缺少内联注释。
- 修改 `structural/composite-pattern.html`：同样为第 6 节 TS 与第 7 节 Python 示例补充逐行中文注解（标注 Component/Leaf/Composite 三角色职责与递归逻辑）；修正第 6 节正文里残留的错误「第 20-23 行」行号引用。
- 新增「桥接 vs 组合」对比内容：在 `composite-pattern.html` 第 9 节后新增 9.1 小节（id=bridge-vs-composite，含四行对比表 + 快速判别法 + 链接回 bridge），并在 `composite` 与 `bridge` 两篇第 9 节对比表各加一行互为锚点链接。深度对比放在 composite 篇（因 bridge 第 9 节已满、composite 第 9 节原本缺桥接），避免重复内容、符合「一篇一模式」约定。
- 修改 `structural/flyweight-pattern.html`：为第 6 节 TS 与第 7 节 Python 正例示例补充逐行中文注解（说明 Flyweight 内部状态共享、外部状态调用时传入、Factory 按 key 复用实例；Python 额外点出 frozen=True 防误改）。❌ 反例代码块原本已带注释，未改动；本篇正文无错误行号引用。

# 2026-07-28

- 修改 `structural/bridge-pattern.html`：为第 6 节 TypeScript 示例与第 7 节 Python 示例补充逐行中文注解（注释说明 Abstraction/Implementor 各角色职责与组合连接方式）；同时修正第 6 节正文里残留的错误「第 34 行」行号引用。AGENTS.md 要求每篇笔记 TS/Python 示例需解释关键类职责，此前该文件代码块缺少内联注释。
- 修改 `structural/composite-pattern.html`：同样为第 6 节 TS 与第 7 节 Python 示例补充逐行中文注解（标注 Component/Leaf/Composite 三角色职责与递归逻辑）；修正第 6 节正文里残留的错误「第 20-23 行」行号引用。
- 新增「桥接 vs 组合」对比内容：在 `composite-pattern.html` 第 9 节后新增 9.1 小节（id=bridge-vs-composite，含四行对比表 + 快速判别法 + 链接回 bridge），并在 `composite` 与 `bridge` 两篇第 9 节对比表各加一行互为锚点链接。深度对比放在 composite 篇（因 bridge 第 9 节已满、composite 第 9 节原本缺桥接），避免重复内容、符合「一篇一模式」约定。
- 修改 `structural/flyweight-pattern.html`：为第 6 节 TS 与第 7 节 Python 正例示例补充逐行中文注解（说明 Flyweight 内部状态共享、外部状态调用时传入、Factory 按 key 复用实例；Python 额外点出 frozen=True 防误改）。❌ 反例代码块原本已带注释，未改动；本篇正文无错误行号引用。
- 行为型教程「内容补强」（用户选定方向，未改整体结构）：给三对易混模式加「对比小节」+ 补全互链，对标结构型的桥接vs组合。① 策略篇加 10.1「策略 vs 状态」(id=strategy-vs-state) 与第10节「命令」行；② 观察者篇加 10.1「观察者 vs 中介者」(id=observer-vs-mediator) 与第10节「中介者」行；③ 命令篇加 8.1「命令 vs 策略」(id=command-vs-strategy)；④ 状态篇第9节「策略」行、中介者篇第9节「观察者」行均改为指向对应锚点。注意：行为型各篇骨架不统一（策略/观察者 11 节且「相似模式」为第10节；状态/中介者/命令 10 节且「相似模式」为第9或8节；表格列数 2/3 列并存），均贴着原骨架插入。剩余易混对（状态vs模板方法、责任链vs命令等）未做，可后续扩展。
- 行为型 11 篇标题/h1 去除「教程」二字（「X模式教程」→「X模式」），与结构型命名（如「桥接模式」）对齐。脚本批量替换后 grep 复检确认 behavioral 目录已无「教程」残留；该词原本仅见于 title 与 h1，正文/链接不受影响。

- 新增概览/导论文章 `fundamentals/why-classify-patterns.html`（用户提议「在设计模式前加一篇解释为什么这样划分 + 简述各模式」，我升级为「三问 mental model」：创建型=对象怎么来 / 结构型=怎么组合 / 行为型=怎么协作）。结构：h1+lead+meta → 今日目标 → 1 为什么分类 → 2 三问模型(.concept 模块 + 内联 SVG 三类概览 + 生活类比) → 3 创建型6模式表 → 4 结构型7模式表 → 5 行为型11模式表 → 6 易混对(桥接vs组合、策略vs状态，各含对比表并锚点回 composite/strategy 对应小节) → 7 选模式速查表 → 8 练习题 → 9 参考答案 → 今日总结。配色用 fundamentals 灰(#475569)；SVG 内三类卡片分别用 patterns-data.js 的蓝/绿/紫强调色。已注册进 `assets/patterns-data.js`（fundamentals 首条，使导航落在三大类模式之前），并在 `learning-progress.md`「前置基础」补行（已学习 2026-07-28）。标题刻意不带「教程」，与用户先前去教程的取向一致。
- 待办（未做）：① 结构型剩余 4 篇（adapter/decorator/proxy/facade）仍缺代码中文注解与 §9 互链，可仿 bridge/composite/flyweight 补齐；② 行为型其余易混对（状态vs模板方法、责任链vs命令、观察者vs中介者已做）可继续加对比小节；③ 概览文章若想更突出，可在 `index.html` hero 区加入口链接（当前仅通过 patterns-data.js 出现在导航）。
